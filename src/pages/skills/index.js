import React from 'react';

import { Block } from '../../ui-kit/Block';
import { Layout } from '../../components/Layout';

const SkillsPage = () => (
  <Layout>
    <div className="SkillsPage">
      <Block>Hi from Skills page!</Block>
    </div>
  </Layout>
);

export default SkillsPage;
